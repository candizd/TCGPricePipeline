# --- API anahtarları (gerçek değerleri .env'e koy, .env commit EDİLMEZ) ---
JUSTTCG_API_KEY=
POKEMONPRICETRACKER_API_KEY=

# --- MinIO (bronze object storage) ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=http://localhost:9000
MINIO_BRONZE_BUCKET=bronze

# --- Kur normalize (F1/F4'te kullanılacak) ---
# Sabit fallback kur; ileride API'dan çekilecek
USD_TRY_FALLBACK=
JPY_USD_FALLBACK=
